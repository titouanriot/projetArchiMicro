export interface Account {
    first_name : string,
    last_name : string,
    email : string,
    birth_date : string,
    password : string,
    is_user_account_active : boolean,
    is_admin : boolean
}